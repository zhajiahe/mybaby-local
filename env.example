# ===========================================
# My Baby - 环境变量配置
# ===========================================

# -------------------------------------------
# 数据库配置 (SQLite)
# -------------------------------------------
# SQLite 数据库文件路径 (相对于项目根目录)
DATABASE_URL="file:../data/baby.db"

# -------------------------------------------
# 对象存储配置 (MinIO)
# -------------------------------------------
# MinIO 服务端点
MINIO_ENDPOINT="http://localhost:9500"

# MinIO 访问密钥 (建议在生产环境使用强密码)
MINIO_ACCESS_KEY="minioadmin"
MINIO_SECRET_KEY="minioadmin"

# MinIO 存储桶名称
MINIO_BUCKET_NAME="my-baby"

# MinIO 公开访问 URL (用于生成文件访问链接)
# 本地开发: http://localhost:9500/my-baby
MINIO_PUBLIC_URL="http://localhost:9500/my-baby"

# MinIO 外部端点 (用于生成浏览器可访问的预签名URL)
# 本地开发和Docker都使用此值
MINIO_EXTERNAL_ENDPOINT="http://localhost:9500"

# -------------------------------------------
# 安全配置
# -------------------------------------------
# 访问密码（可选，留空则不启用密码保护）
# 设置后需要输入密码才能访问应用，认证有效期 7 天
ACCESS_PASSWORD=

# -------------------------------------------
# 可选配置
# -------------------------------------------

# FFmpeg 路径 (如果不在 PATH 中)
# FFMPEG_PATH="/usr/bin/ffmpeg"
# FFPROBE_PATH="/usr/bin/ffprobe"

# 应用 URL (可选)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# -------------------------------------------
# Docker Compose 专用配置 (自动配置)
# -------------------------------------------
# docker-compose.yml 中配置的环境变量:
# - DATABASE_URL=file:/app/data/baby.db
# - MINIO_ENDPOINT=http://minio:9000 (容器内部通信)
# - MINIO_PUBLIC_URL=http://localhost:9500/my-baby (外部访问)
